# Netlify build settings for By-Bookey (Vite + React Router SPA)
[build]
  command = "npm run build"
  publish = "dist"

# Client-side routing — same rule as public/_redirects
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
